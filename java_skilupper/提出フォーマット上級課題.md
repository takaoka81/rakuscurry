カテゴリーC
```
課題番号：6
① 該当：あり
② 場所：OrderService.java -> paymentMethodJudge() (74~90行目)
③ 問題と修正の要約
　支払い方法が増えるたびにif/switch文の分岐を都度修正する必要があったため、支払い処理を PaymentProcessor インターフェースとして定義し、pay()メソッドで支払い方法ごとにOrderのstatus（注文状態）を返す実装（CreditCardPaymentProcessor / CodPaymentProcessor）に切り出した
```

```
課題番号：7
① 該当：あり
② 場所：OrderService.java -> order() (60~65行目)
③ 問題と修正の要約
　単一責任原則の違反により、処理の呼び出し順を変えたり処理を追加したりする際に既存処理へ影響が及ぶ可能性があったため、本来の処理である注文確定時のDB更新はそのままにし、スタンプ更新・無料カレー適用の注文アイテム更新・メール送信用のオブジェクト再取得といった付随処理はApplicationEventPublisherでイベント発行し、各リスナーで実行するように変更した
```

```
課題番号：12
① 該当：あり
② 場所：InsertForm.java -> zipcode, telephone (28~29, 35~36行目)
       OrderForm.java -> destinationZipcode, destinationTel (25~26, 32~33行目)
③ 問題と修正の要約
　各フォームで個別に@Patternと正規表現を書くとクラスごとにメッセージやパターンがずれる可能性があるため、郵便番号・電話番号の形式チェックを@PostalCode・@PhoneNumberというカスタムアノテーション（PostalCodeValidator/PhoneNumberValidator）に切り出し、両フォームで統一されたバリデーションを行うように実装した
```

```
課題番号：17
① 該当：あり
② 場所：CartService.java -> insertOrderItem() (108~111行目)
       CartService.java -> insertOrderTopping() (120~126行目)
       CartController.java -> inCart() (50~51行目)
③ 問題と修正の要約
　CartItem・OrderItem・OrderToppingをフォームやカート情報から新規に組み立てる際、`BeanUtils.copyProperties()`だと型不一致やプロパティ名の差異で必要な情報が漏れる可能性があり、また金額計算などの変換処理が呼び出し側に散らばるため、各Domainクラスに静的ファクトリーメソッド（`form()`）を実装し、生成処理の責任をDomainクラス側に持たせる実装にした。なお、注文確定時の`Order`更新（OrderController）は既存のOrderに対する一部項目の上書きに留まるため対象外とし、`BeanUtils.copyProperties()`のまま残している
```

```
課題番号：25
① 該当：あり
② 場所：SimpleMailService.java -> sendMail() (51~71行目)
③ 問題と修正の要約
　メール送信失敗時の再試行（リトライ）機能や 送信ログ機能を追加する際に`sendMail()`に直接追加するとコードの条件分岐が複雑になるためDecorator パターンを使ってメールの機能別にクラスを分けてメール送信機能を作成する実装にした
```

```
課題番号：14
① 該当：なし
② 場所：
③ 問題と修正の要約
　件数が少ない状態ではテーブルを全スキャンしてもレスポンスに差はあまりないが、件数が多くなるとレスポンスが遅くなるため`INDEX`を追加することで指定された情報のみスキャンするように実装した
```

```
課題番号：16
① 該当：あり
② 場所：CartController.java -> addItems() (142~171行目)
③ 問題と修正の要約
カートに商品を追加する処理がループしていたため、商品情報・トッピング情報の取得や合計金額の再計算といったSQLが商品の数だけ繰り返し実行されていた。商品情報・トッピング情報をそれぞれループの前に1回だけまとめて取得しMapに保持することで、ループ内でのSQL発行をなくした。また、カートの存在確認と合計金額の更新もループの外に出し、全商品登録後に1回だけ実行するよう変更することでSQLの実行回数を減らした。
```
