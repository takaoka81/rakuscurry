# 提出フォーマット

各課題を終えたら、以下の形式で提出してください。

カテゴリーA
```
課題番号：2
① 該当：あり
② 場所：ItemRepository.java > findByName()（60行目）
        OrderRepository.java > findByOrdertable(Integer userId)(220行目)
③ 問題と修正の要約（2〜3行で）
  検索結果が０件の時にnullを返していた。Listを返すメソッドはnullではなく空リストを返すべきなので、itemList(ItemRepository.java),orderList(OrderRepository.java)に変更した。
```

```
課題番号：2
① 該当：あり
② 場所：ItemRepository.java > showItemDetail(Integer id)（73行目）
        ItemRepository.java > ApiShowItemDetail(Integer id)（102行目）
        OrderRepository.java > findByUserIdAndStatus(Integer userId, Integer status)（276行目）
        UserRepository.java > findByUserId(Integer id)（40行目）
        UserRepository.java > findByMailAddress(String email)（75行目）
③ 問題と修正の要約（2〜3行で）
  検索結果が０件の時にnullを返す恐れがあったのでOptional<T>を返すようにした。
```

```
課題番号：2
① 該当：あり
② 場所：UserRepository.java > findByMailAddress(String email)（79~80行目）
③ 問題と修正の要約（2〜3行で）
  エラー時にnullを返していたのでthrowするようにした
```

```
課題番号：8
① 該当：あり
② 場所：LoginUserDetailsService.java > loadUserByUsername(String email)（21行目）
③ 問題と修正の要約（2〜3行で）
  デバック用の`System.out.println`でメールアドレスを表示していたので削除した。
```
```
課題番号：8
① 該当：あり
② 場所：CartService.java > addItemToCart(CartItem cartItem, Integer userId)（130行目）
③ 問題と修正の要約（2〜3行で）
  デバック用の`System.out.println`をloggerに変更した。
```
```
課題番号：8
① 該当：あり
② 場所：  OrderService.java > order(Order order, User user, StampHistory stampHistory)（117行目）
③ 問題と修正の要約（2〜3行で）
  ①の注文ステータス更新後に、②や③でエラーが発生すると、注文ステータスのみ更新された状態となり、スタンプ情報の整合性が取れなくなるため、トランザクション管理が必要である。
```

```
課題番号：8
① 該当：あり
② 場所：OrderForm.java > getTimestamp()（106,109行目）
③ 問題と修正の要約（2〜3行で）
  Formクラスはデータを保持するクラスであるためgetTimestamp()メソッドは削除してOrderServiceクラスに移動した。
```

```
課題番号：8
① 該当：あり
② 場所：OrderService.java > parseDeliveryTime(String deliveryTime)（123,126行目）
③ 問題と修正の要約（2〜3行で）
  コンソールにのみ出力されていたエラーログやデバッグ用のコードを、SLF4Jロガーを利用したログ出力へ置き換えることで、ログファイルにも記録されるよう改善しました。
```
```
課題番号：8
① 該当：あり
② 場所：InsertController.java > insert()（100行目）
③ 問題と修正の要約（2〜3行で）
  コンソールにのみ出力されていたエラーログやデバッグ用のコードを、SLF4Jロガーを利用したログ出力へ置き換えることで、ログファイルにも記録されるよう改善しました。
```
```
課題番号：8
① 該当：あり
② 場所：UserController.java > updateUser()（111行目）
③ 問題と修正の要約（2〜3行で）
  コンソールにのみ出力されていたエラーログやデバッグ用のコードを、SLF4Jロガーを利用したログ出力へ置き換えることで、ログファイルにも記録されるよう改善しました。
```

```
課題番号：3
① 該当：あり
② 場所：CartService.java > addItemToCart() （74~81,113~116行目）
        ItemService.java > showItemDetail() (52行目)
        ItemService.java > ApiShowItemDetail (100行目)
③ 問題と修正の要約（2〜3行で）
  Repositoryの戻り値がOptional<T>に変更に伴いOptional．mapを使うようにした
```

```
課題番号：3
① 該当：あり
② 場所：CartService.java > addItemToCart() （123行目）
③ 問題と修正の要約（2〜3行で）
  コンソールにのみ出力されていたエラーログやデバッグ用のコードを、SLF4Jロガーを利用したログ出力へ置き換えることで、ログファイルにも記録されるよう改善しました。
```

```
課題番号：3
① 該当：あり
② 場所：OrderService.java > order() （93行目）
③ 問題と修正の要約（2〜3行で）
  OrderService全体にTransactionalが適応されていたので、orderメソッドにのみ適応するようにした。
```

```
課題番号：3
① 該当：なし
② 場所：`List` フィールドは `null` にならない設計にする
③ 問題と修正の要約（2〜3行で）
  意図せずにList型がnullになる可能性があるのでフィールド変数定義時にnew ArrayList<>()で初期化して、setterは作らない
```

```
課題番号：19
① 該当：あり
② 場所：OrderService.java -> RowMapper() (23行目)
        OrderService.java -> existsByMailAddress() (76,80行目)
        OrderService.java -> insert() (96行目)
        OrderService.java -> delete() (139行目)
③ 問題と修正の要約（2〜3行で）
  ユーザーの論理削除のステータスを数字で管理していたがわかりずらいためEnum化してわかりやすいようにした。
```

```
課題番号：19
① 該当：あり
② 場所：UserRepository.java -> paymentMethodJudge() (140~143行目)
③ 問題と修正の要約（2〜3行で）
  支払い方法とステータスを数字で管理していたがわかりずらいためEnum化してわかりやすいようにした。
```

```
課題番号：24
① 該当：あり
② 場所：OrderItem.java -> getSubTotal() (128行目)
③ 問題と修正の要約（2〜3行で）
  discountにnullがチェックが抜けていたのでnullチェックを追加した
```

```
課題番号：24
① 該当：あり
② 場所：domainパッケージ内のInteger型フィールドを持つ各クラス（CartItem, Item, Order, OrderItem, OrderTopping, StampHistory, Topping, User）
③ 問題と修正の要約
　DBカラムにNOT NULL制約がありnullが入り得ないにもかかわらず、対応するdomainクラスのフィールドをInteger型（null許容）で保持しており、unboxing時のNPEリスクが不要に残っていた。該当フィールドをプリミティブのint型に変更し、null非許容であることを型レベルで保証した。
```


カテゴリーB
```
課題番号：23
① 該当：あり
② 場所：OrderRepository.java -> orderLoad() (170行目)
  　    OrderRepository.java -> findByOrdertable() (202行目)
  　    findByUserId.java -> findByOrdertable() (43~48行目)
③ 問題と修正の要約
　DBから情報を取得する際に不要な情報まで取得してフロントに返していたので不要な情報はDBから取得してないようにした。
```

カテゴリーC
```
課題番号：1
① 該当：あり
② 場所：OrderService.java -> paymentMethodJudge() (141,143行目)
③ 問題と修正の要約
　ユーザーのステータスを数字で管理していたがわかりずらいためEnum化してわかりやすいようにした。
```

```
課題番号：4
① 該当：あり
② 場所：InsertController.java > insert(...)（85行目付近）
③ 問題と修正の要約
　`new User.Builder().toString()`を各フィールドをチェーンで設定し`.build()`で`User`を生成する形に修正した。
```

```
課題番号：4
① 該当：あり
② 場所：UserController.java > updateUser(...)（92〜100行目）
③ 問題と修正の要約
　プロフィール更新処理を`new User.Builder()`から作っていたため、フォームの5項目（name, email, zipcode, address, telephone）以外の`password`・`stampNowCount`・`stampAllCount`が引き継がれずnullになる不具合があった。加えてBuilder化した後もsetterを併用していたため`build()`する形に修正した。
```

```
課題番号：22
① 該当：あり
② 場所：CartService.java > adaptFreeCurry(Order order, Integer userId)（旧139〜193行目）
③ 問題と修正の要約
　無料枠取得・無料適用・合計金額再計算・トッピング集計という複数の処理が1メソッドに詰め込まれ、可読性・テスト容易性が低かった。`getFreeCount`・`applyFreeDiscount`・`calcTotalWithDiscount`・`calcToppingTotal`の4メソッドに分割し、`adaptFreeCurry`は各処理を呼び出すだけのオーケストレーション役に整理した。
　分割した4メソッドは同一パッケージのテストからのみ呼び出す想定のため、`public`ではなくpackage-privateなアクセス修飾子とした。
```

```
課題番号：22
① 該当：あり
② 場所：OrderService.java > sendMail(Order order, String to)（旧162〜228行目）
③ 問題と修正の要約
　テンプレート読み込み・注文商品情報の構築・プレースホルダー置換・メール送信という複数の処理が1メソッドに詰め込まれていた。`loadMailTemplate`・`buildOrderItems`・`buildMailBody`・`sendMailMessage`の4メソッドに分割し、`sendMail`は各処理を呼び出すだけのオーケストレーション役に整理した。
```

カテゴリーD
```
課題番号：18
① 該当：あり
② 場所：全対象クラス(Repository,Service,Controller)
③ 問題と修正の要約
　@Autowiredによるフィールドインジェクションを廃止し、
全対象クラスに@RequiredArgsConstructor（Lombok）を付与し、フィールドをprivate final化してコンストラクタインジェクションに統一
```

```
課題番号：27
① 該当：あり
② 場所：OrderService.java -> loadMailTemplate() (186~191行目)
③ 問題と修正の要約
　InputStreamがエラー発生時に閉じられないためtry-with-resourcesを実装することでtryを抜ける時に自動でcloseされるように変更した。
```

```
課題番号：20
① 該当：あり
② 場所：Order.java・OrderItem.java・CartItem.java・Item.java > List/Dateを持つgetter・setter全般
③ 問題と修正の要約
　`orderItemList`・`orderTopping`・`toppingList`（List）や`orderDate`（Date）のgetter/setterが内部の可変オブジェクトの参照をそのまま返す・受け取っていたため、呼び出し側で中身を変更すると内部状態が意図せず書き換わる防御的コピー漏れがあった。getterは`Collections.unmodifiableList`または`List.copyOf`／`new Date(...)`で複製を返し、setterも`List.copyOf`／`new Date(...)`で複製を保持するよう修正した。
```

```
課題番号：21
① 該当：あり
② 場所：User.java, CartItem.java, Item.java, Order.java, OrderItem.java, OrderTopping.java, StampHistory.java, Topping.java, LoginUserDetails.java > 各クラス全般
③ 問題と修正の要約
　これら9つのドメインクラスでequals/hashCodeが未実装だったため、デフォルト実装（参照の同一性のみで比較）のままだった。そのためDBから取得した内容が同じインスタンス同士でも別物と判定され、テストの`assertEquals`が意図通り通らない、`List`同士の内容比較（`orderItemList`や`toppingList`など入れ子のList比較）が正しく機能しない、`HashSet`/`HashMap`のキーとして使った際に重複排除や検索ができない、といった不具合につながっていた。全フィールドを対象にequals/hashCodeを実装して解消した。
```

```
課題番号：28
① 該当：あり
② 場所：CartController.java（45,61,84,124行目付近）、ItemController.java、SecurityConfig.java > customSuccessHandler()、OrderController.java（217行目）
③ 問題と修正の要約
　`HttpSession#getAttribute`/`ServletContext#getAttribute`はObject型しか返せず、`List<CartItem>`・`List<Topping>`として使うために`@SuppressWarnings("unchecked")`付きのuncheckedキャストを各所で行っていた。`toppingList`も`ServletContext`経由をやめ`ItemService.findAllTopping()`を直接呼ぶよう変更し、`item_detail.html`・`cart_list.html`のテンプレート参照も合わせて修正した。
```

カテゴリーE
```
課題番号：9
① 該当：あり
② 場所：InsertController.java > insert()（99行目）
③ 問題と修正の要約
　メールアドレス重複時の例外処理で`e.printStackTrace()`によりスタックトレースを標準エラー出力に直接出しており、本番運用では不適切だった。既存の`logger`（SLF4J）を用いた`logger.error("そのメールアドレスはすでに使われています", e)`に置き換えた。
```

```
課題番号：10
① 該当：あり
② 場所：OrderService.java > メール関連メソッド
③ 問題と修正の要約
  処理の役割が異なるためメールの関連メソッドを'OrderService.java'から'MailService.java'に移動して、それに伴い'import'文の追加を行った。
```

```
課題番号：38
① 該当：あり
② 場所：OrderController.java > orderCo() (104~122行目)
③ 問題と修正の要約
  メソッドが使われておらず、想定の処理が別のメソッドで実装されており、デッドコードとなっていたため削除した。
```

```
課題番号：38
① 該当：あり
② 場所：CartService.java > calcTotalWithDiscount(List<OrderItem>)（241〜243行目）
③ 内容の要約
　`Integer subPrice = 0;`で初期化した直後、一度も読まれないまま無条件に上書きしていたデッドストアがあった。宣言と代入を1文にまとめて無駄な初期化を解消した。
```

```
課題番号：11
① 該当：あり
② 場所：CartController.java > showCart()（82~85行目）
③ 内容の要約
　`for` ループによる合計金額の計算を `Stream` に変換した
```

```
課題番号：11
① 該当：あり
② 場所：CartService.java > insertOrderTopping()（127~134行目）
③ 内容の要約
　`for` ループによるトッピングのリスト化を `Stream` に変換した
```

カテゴリーF
```
課題番号：29
① 該当：あり
② 場所：ItemRepository.java > findByName()（58,74,105行目）
        ToppingRepository.java > findAllTopping() (39行目)
        UserRepository.java > findByMailAddress() (72行目)
③ 問題と修正の要約
　SELECT * だと存在しないカラムのエラーが、アプリケーションが起動したときではなくSQLを実行したときに初めてわかるため実際のカラム名で書くように変更した
```

```
課題番号：30
① 該当：あり
② 場所：OrderRepository.java > orderLoad()（174,175行目）
        OrderRepository.java > findByOrdertable() (206,207行目)
③ 問題と修正の要約
  　RIGHT JOINで結合していたため、ordersテーブルではなくorder_items／itemsテーブル側が結合の起点になり、
　明細（order_items）が存在しない注文が結果から漏れる問題があった。ordersテーブルを起点に統一するため、LEFT JOINに変更した。
```

```
課題番号：16
① 該当：あり
② 場所：OrderToppingRepository.java > updateOrder()（75~78, 89行目）
        CartService.java > order() (96~103行目)
③ 問題と修正の要約
  　カートに商品を追加する際、選択したトッピングの数だけ`orderToppingRepository.insert`をループ内で1件ずつ呼び出しており、SQLがトッピング数分発行されるN+1（書き込み側）の問題があった。`OrderToppingRepository.insert`の引数を`List<OrderTopping>`に変更し`NamedParameterJdbcTemplate#batchUpdate`でまとめて登録するようにし、`CartService`側もリストを組み立てて1回だけ呼び出す形に修正した。
```

```
課題番号：16
① 該当：あり
② 場所：OrderItemRepository.java > update・updateOrder
        OrderService.java > order（90〜110行目）
        Cartervice.java > calcTotalWithDiscount（237〜249行目）
③ 問題と修正の要約
  割引額（discount）反映処理と0円適用時のorder_price更新処理が、それぞれ商品数分ループ内で`orderItemRepository`のUPDATEを1件ずつ呼び出しておりSQLが多重発行される問題があった（書き込み側N+1）。`OrderItemRepository`の`update`・`updateOrder`をそれぞれ`List<OrderItem>`を受け取り`NamedParameterJdbcTemplate#batchUpdate`でまとめて更新する形に変更し、両サービスともループ終了後に1回だけ呼び出す形に修正した。また修正途中でUPDATE呼び出しがループ内に残っていた点、更新対象カラムが異なる`update`（discount）と`updateOrder`（order_price）の呼び分けが誤っていた点も併せて修正した。
```

```
課題番号：31
① 該当：あり
② 場所：DB　ordersテーブルのstatus,payment_method usersテーブル　status
③ 内容の要約
　`orders`テーブルの`status`,`payment_method` `users`テーブル　`status`にCHECK制約が抜けていたため不正な値を入力することが可能で合ったためCHECK制約を追加した
```

カテゴリーG
```
課題番号：13
① 該当：あり
② 場所：動的フォームの作成 add_item.js新規作成
③ 内容の要約
　カート画面から商品を追加することができなかったのでカートに商品があればカート画面からでも商品を追加できるようにした。
```
